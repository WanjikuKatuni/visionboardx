// set up project theme

import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        mode: "dark",
        background: {
            default: "#1d1f26"
        }
    }
})

export default theme;